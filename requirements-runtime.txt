# Runtime dependencies actually imported by agents/, baselines/, cran_env/,
# evaluation/, training/ (verified by grepping every import in those
# packages — see docs/rules.md Rule 4). This is the lean subset installed
# into the Docker image (Dockerfile); requirements.txt is the fuller local
# development environment (notebooks, lint, experiment tracking) and is not
# needed to run or reproduce an experiment.
#
# Pinned to the exact versions the full test suite (45/45) passed against
# on 2026-08-05. torch is a CPU-only wheel by default; for a CUDA image,
# override with:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu121

torch==2.13.0
gymnasium==1.3.0
numpy==2.4.6
scipy==1.17.1
matplotlib==3.11.1
PyYAML==6.0.1

# Used by baselines/convex_power.py, guarded by a try/except fallback to
# scipy.optimize.linprog if unavailable — not empirically verified in this
# session, pinned to a recent stable release.
cvxpy==1.5.3

# Not imported by any experiment/training code path, but kept so the image
# can also run the test suite as a build-time or deploy-time smoke check.
pytest==9.1.1
